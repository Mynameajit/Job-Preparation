import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Problem Solving",
};

export default function ProblemSolvingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
